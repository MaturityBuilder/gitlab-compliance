# Depth 3 leaf — deepest build job.

variables:
  PUBLISH_VAR: "from-publish"

publish_image:
  stage: deploy
  image: alpine:3.20.0
  script:
    - echo "publish image"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
