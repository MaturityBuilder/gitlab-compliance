# Generated by generate.py — mid-hop 06 include + themed job.
include:
  - local: layer-07.yml

ansible_lint:
  stage: lint
  variables:
    LAYER_HOP: "6"
  script:
    - set -euo pipefail
    - ansible-lint --version
    - ansible-lint "$CI_PROJECT_DIR/playbooks/"
