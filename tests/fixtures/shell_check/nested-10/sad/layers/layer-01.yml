# Generated by generate.py — mid-hop 01 include + themed job.
include:
  - local: layer-02.yml

tflint:
  stage: lint
  variables:
    LAYER_HOP: "1"
  script:
    - set -euo pipefail
    - echo "tflint hop=1"
    - tflint --chdir="$CI_PROJECT_DIR"
