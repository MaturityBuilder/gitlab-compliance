# Generated by generate.py — mid-hop 09 include + themed job.
include:
  - local: layer-10.yml

semgrep_sast:
  stage: security
  variables:
    LAYER_HOP: "9"
  script:
    - set -euo pipefail
    - semgrep --config=p/ci --error "$CI_PROJECT_DIR"
