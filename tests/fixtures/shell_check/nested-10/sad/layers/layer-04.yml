# Generated by generate.py — mid-hop 04 include + themed job.
include:
  - local: layer-05.yml

hadolint:
  stage: lint
  variables:
    LAYER_HOP: "4"
  script:
    - set -euo pipefail
    - hadolint --version
    - hadolint "$CI_PROJECT_DIR/Dockerfile"
