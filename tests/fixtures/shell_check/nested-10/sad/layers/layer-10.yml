# Generated by generate.py — hop 10 leaf (job defined only here).
nested_leaf_sad:
  stage: verify
  script:
    - echo "$UNQUOTED"
    - cd "$HOME"
    - value=$(echo hi)
    - apk add curl
    - pip install requests
    - docker pull nginx
    - check=$(curl $HOSTNAME | jq -r '.message')
    - echo "$PREFIX"$SUFFIX
