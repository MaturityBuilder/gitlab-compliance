# Generated by generate.py — mid-hop 08 include + themed job.
include:
  - local: layer-09.yml

gitleaks:
  stage: security
  variables:
    LAYER_HOP: "8"
  script:
    - set -euo pipefail
    - gitleaks detect --source="$CI_PROJECT_DIR" --no-git
