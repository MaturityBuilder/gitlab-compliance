# Generated by generate.py — mid-hop 07 include + themed job.
include:
  - local: layer-08.yml

checkov_scan:
  stage: security
  variables:
    LAYER_HOP: "7"
  script:
    - set -euo pipefail
    - checkov -d "$CI_PROJECT_DIR/terraform" --framework terraform
