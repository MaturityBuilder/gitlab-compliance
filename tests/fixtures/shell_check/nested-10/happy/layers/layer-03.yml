# Generated by generate.py — mid-hop 03 include + themed job.
include:
  - local: layer-04.yml

docker_scan:
  stage: security
  variables:
    LAYER_HOP: "3"
  script:
    - set -euo pipefail
    - docker run --rm aquasec/trivy:0.50.1 image "python:3.12.0"
