# Generated by generate.py — mid-hop 02 include + themed job.
include:
  - local: layer-03.yml

terraform_plan:
  stage: plan
  variables:
    LAYER_HOP: "2"
  script:
    - set -euo pipefail
    - terraform version
    - terraform plan -input=false -out="$CI_PROJECT_DIR/plan.cache"
