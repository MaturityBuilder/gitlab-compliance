# Generated by generate.py — mid-hop 05 include + themed job.
include:
  - local: layer-06.yml

kube_linter:
  stage: lint
  variables:
    LAYER_HOP: "5"
  script:
    - set -euo pipefail
    - kube-linter lint "$CI_PROJECT_DIR/deploy/"
